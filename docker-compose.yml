services:
  db:
    image: postgres:15-alpine
    container_name: emprendimiento-db
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "${DB_PORT}:5432"

  backend:
    build: 
      context: ./backend
    container_name: emprendimiento-backend
    restart: always
    ports:
      - "8080:8080"
    environment:
      DB_HOST: db 
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DB_PORT: 5432
      JWT_SECRET: ${JWT_SECRET}
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASS: ${SMTP_PASS}
    depends_on:
      - db

  frontend:
    build:
      context: ./frontend
    container_name: emprendimiento-frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend

  admin-frontend:
    build:
      context: ./admin-frontend
    container_name: emprendimiento-admin-frontend
    restart: always
    ports:
      - "4200:80"
    depends_on:
      - backend

volumes:
  postgres_data: